{
  "_comment": "Deprecated example. The SQLite `project` table (schema v17) is the only source of truth for the project registry. Add projects via the dashboard Projects page or POST /api/projects. This file is ignored at boot — kept only as documentation for operators migrating from a previous install.",
  "_deprecated_projects": [
    {
      "_note": "Example deployment: the `forge` project pointing at Forge's own repo, so the orchestrator builds itself. Add via the UI, not here.",
      "id": "forge",
      "name": "Forge",
      "repoUrl": "https://github.com/Xyrces/Forge.git",
      "defaultBranch": "main"
    },
    {
      "_note": "Example project: any other repo. The orchestrator doesn't care that it's PortHorizon — that's historical context.",
      "id": "porthorizon",
      "name": "PortHorizon",
      "repoUrl": "https://github.com/Xyrces/PortHorizon.git",
      "defaultBranch": "main"
    }
  ]
}
